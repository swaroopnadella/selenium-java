package launchBrowsers;

import org.openqa.selenium.WebDriver;

public class LaunchWebDriver {

	public static void main(String[] args) {
		
		//WebDriver driver = new WebDriver();
		//uncomment the above line to get compile time error "Cannot instantiate the type WebDriver"
		//It is not possible to create object for WebDriver which is an Interface
		
		
	}

}
